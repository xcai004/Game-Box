import React, { Fragment } from "react";
import { View, Text, YellowBox } from "react-native";
import Navigation from "./src/Navigation/Navigation";

YellowBox.ignoreWarnings(["Remote de"]);

const App = () => {
  return <Navigation />;
};

// const styles = StyleSheet.create({

// });

export default App;
