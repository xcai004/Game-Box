import React, { Component } from "react";
import { View, Text } from "react-native";

const GameSnake = () => {
  return (
    <View>
      <Text>Game Snake</Text>
    </View>
  );
};

GameSnake.navigationOptions = {
  title: "Game Sname"
};

export default GameSnake;